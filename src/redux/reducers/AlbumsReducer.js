import { albumsActionTypes } from "../actions/AlbumsActions";

const initialState = {
  albumsList: null
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
